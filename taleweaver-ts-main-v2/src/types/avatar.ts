export interface AvatarProps {
  id: string;
  name: string;
  alias: string;
  description: string;
}

export interface AvatarRequestProps {
  theme: string;
  title: string;
  description: string;
}
